//question 30
let userNames = ['aliza', 'arisha', 'sabahat', 'saima', 'abid'];
userNames.forEach((userName) => {
    if (userName == "aliza") {
        console.log("hello Aliza, would you practice a tsc on today?");
    }
    else {
        console.log(`hello ${userName}, would you practice a tsc on today?`);
    }
});
export {};
